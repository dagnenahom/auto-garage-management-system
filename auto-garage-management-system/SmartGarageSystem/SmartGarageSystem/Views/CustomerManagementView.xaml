<UserControl x:Class="SmartGarageSystem.Views.CustomerManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search bar -->
        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <Button Content="Refresh" Command="{Binding RefreshCommand}" DockPanel.Dock="Right" Width="80" Margin="5,0,0,0"/>
            <Button Content="Clear" Command="{Binding ClearSearchCommand}" DockPanel.Dock="Right" Width="60" Margin="5,0,0,0"/>
            <Button Content="Search" Command="{Binding SearchCommand}" DockPanel.Dock="Right" Width="70"/>
            <Label Content="Search:" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30" Width="685" VerticalContentAlignment="Stretch"/>
           
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Customer list -->
            <GroupBox Grid.Column="0" Header="Customers" Margin="0,0,10,0">
                <ListBox ItemsSource="{Binding Customers}" 
                         SelectedItem="{Binding SelectedCustomer}" 
                         DisplayMemberPath="FullName"/>
            </GroupBox>

            <!-- Edit form -->
            <GroupBox Grid.Column="1" Header="Edit Customer">
                <StackPanel Margin="10">
                    <TextBlock Text="{Binding FormMessage}" Foreground="Red" 
                               Visibility="{Binding FormMessage, Converter={StaticResource BoolToVis}}" 
                               TextWrapping="Wrap" Margin="0,0,0,10"/>

                    <TextBlock Text="Full Name *"/>
                    <TextBox Text="{Binding EditFullName, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Phone" Margin="0,10,0,0"/>
                    <TextBox Text="{Binding EditPhone, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Email" Margin="0,10,0,0"/>
                    <TextBox Text="{Binding EditEmail, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Address" Margin="0,10,0,0"/>
                    <TextBox Text="{Binding EditAddress, UpdateSourceTrigger=PropertyChanged}"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                        <Button Content="New" Command="{Binding NewCustomerCommand}" Width="70" Margin="5,0"/>
                        <Button Content="Save" Command="{Binding SaveCustomerCommand}" Width="70" Margin="5,0"/>
                        <Button Content="Delete" Command="{Binding DeleteCustomerCommand}" Width="70" Margin="5,0"/>
                        <Button Content="Cancel" Command="{Binding CancelEditCommand}" Width="70" Margin="5,0"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>