<UserControl x:Class="SmartGarageSystem.Views.CustomerVehicleJobReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartGarageSystem.Views"
        mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <Grid Margin="0,0,0,15">
                <TextBlock Text="Customer Vehicle &amp; Job card status Report" FontSize="28" FontWeight="Bold" Foreground="#2C3E50"/>
                <Button Content="Refresh" Command="{Binding RefreshCommand}" 
                        HorizontalAlignment="Right" Width="80" Height="30"/>
            </Grid>

            <!-- Loading -->
            <TextBlock Text="Loading..." Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" 
                       FontSize="16" Foreground="Gray" HorizontalAlignment="Center" Margin="0,10,0,0"/>

            <!-- Report content -->
            <ItemsControl ItemsSource="{Binding Report.Customers}" Margin="0,10,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Expander Margin="0,0,0,10" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                                  Effect="{DynamicResource CardShadow}">
                            <Expander.Header>
                                <TextBlock Text="{Binding FullName}" FontSize="18" FontWeight="SemiBold" Margin="5,2"/>
                            </Expander.Header>
                            <StackPanel Margin="15,5,5,5">
                                <ItemsControl ItemsSource="{Binding Vehicles}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="10,5" BorderBrush="#ECF0F1" BorderThickness="0,0,0,1">
                                                <StackPanel>
                                                    <TextBlock FontWeight="SemiBold">
                                                        <Run Text="{Binding LicensePlate}"/> - <Run Text="{Binding Make}"/> <Run Text="{Binding Model}"/>
                                                    </TextBlock>
                                                    <!-- Job Cards for this vehicle -->
                                                    <ItemsControl ItemsSource="{Binding JobCards}" Margin="20,5,0,0">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Padding="5,3" Background="#F8F9FA" Margin="0,2">
                                                                    <Grid>
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="Auto"/>
                                                                            <ColumnDefinition Width="*"/>
                                                                            <ColumnDefinition Width="Auto"/>
                                                                        </Grid.ColumnDefinitions>
                                                                        <TextBlock Text="{Binding Status}" FontWeight="Bold" 
                                                                                   Foreground="{Binding Status, Converter={StaticResource StatusColorConverter}}" 
                                                                                   Width="80"/>
                                                                        <TextBlock Grid.Column="1" Text="{Binding Description}" 
                                                                                   TextTrimming="CharacterEllipsis" Margin="10,0"/>
                                                                        <TextBlock Grid.Column="2" Text="{Binding DateCreated}" 
                                                                                   Foreground="Gray" Margin="5,0"/>
                                                                    </Grid>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>