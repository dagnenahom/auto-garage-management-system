<UserControl x:Class="SmartGarageSystem.Views.StockReportView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:local="clr-namespace:SmartGarageSystem.Views"
            mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <Grid Margin="0,0,0,15">
                <TextBlock Text="Stock Report" FontSize="28" FontWeight="Bold" Foreground="#2C3E50"/>
                <Button Content="Refresh" Command="{Binding RefreshCommand}" 
                        HorizontalAlignment="Right" Width="80" Height="30"/>
            </Grid>

            <!-- Summary cards -->
            <WrapPanel Margin="0,0,0,20">
                <Border Background="White" CornerRadius="10" Width="200" Height="100" Margin="0,0,15,15"
                        Effect="{DynamicResource CardShadow}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Report.TotalItemsCount}" FontSize="36" FontWeight="Bold" Foreground="#2980B9" HorizontalAlignment="Center"/>
                        <TextBlock Text="Total Items" FontSize="14" Foreground="#7F8C8D" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Background="White" CornerRadius="10" Width="200" Height="100" Margin="0,0,15,15"
                        Effect="{DynamicResource CardShadow}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Report.LowStockCount}" FontSize="36" FontWeight="Bold" Foreground="#E74C3C" HorizontalAlignment="Center"/>
                        <TextBlock Text="Low Stock" FontSize="14" Foreground="#7F8C8D" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Background="White" CornerRadius="10" Width="200" Height="100" Margin="0,0,15,15"
                        Effect="{DynamicResource CardShadow}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Report.TotalInventoryValue, StringFormat=C}" FontSize="28" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Center"/>
                        <TextBlock Text="Total Value" FontSize="14" Foreground="#7F8C8D" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <!-- Low stock list -->
            <TextBlock Text="Low Stock Items" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <Border Background="White" CornerRadius="10" Padding="15" Effect="{DynamicResource CardShadow}" Height="286">
                <ItemsControl ItemsSource="{Binding Report.LowStockItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="8,5" BorderBrush="#ECF0F1" BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding PartName}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding QuantityInStock, StringFormat='Qty: {0}'}" Margin="20,0" Foreground="#E67E22"/>
                                    <TextBlock Grid.Column="2" Text="{Binding UnitPrice, StringFormat=C}" Foreground="#7F8C8D"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>

            <!-- Loading indicator -->
            <TextBlock Text="Loading..." Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" 
                       FontSize="16" Foreground="Gray" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>